# ── Core ──────────────────────────────────────────────────────────────────────
google-genai>=1.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
jinja2>=3.1.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# ── File parsing ───────────────────────────────────────────────────────────────
PyPDF2>=3.0.0
python-docx>=1.1.0

# ── RAG — Pinecone + embeddings ────────────────────────────────────────────────
pinecone>=3.0.0
sentence-transformers>=2.7.0   # all-MiniLM-L6-v2 (384-dim, ~90 MB)

# ── NLP classifier ─────────────────────────────────────────────────────────────
# transformers + torch are needed for the zero-shot HuggingFace model.
# The rule-based classifier works without them; they are downloaded on first use.
transformers>=4.40.0
torch>=2.0.0                   # CPU-only is fine; ~2 GB install
